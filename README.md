# Cyberellum DNA.ME

Production React frontend for `https://genomic.axiom-x.us/`.

The browser uses the governed same-origin `/api` service on webserver1. The repository contains no third-party application runtime, edge-function runtime, hosted database client, hosted authentication client, or hosted storage client.

## Build

```bash
npm ci
npm run build
```
